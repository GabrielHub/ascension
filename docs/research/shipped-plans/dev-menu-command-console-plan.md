# Dev Menu Command Console Plan

Archived as shipped on 2026-04-02.

The dev menu now ships as a typed command console with shared command discovery, structured result output, browser-test-friendly selectors, command history, and runtime-safe setup paths. Future tooling work should extend the existing command registry and browser-testing contract rather than reopening the retired button-sheet menu model.
