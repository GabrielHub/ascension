import { soa } from "bitecs";

export const OperatorDisposition = soa({
  operatorId: [] as string[],
  sociability: [] as number[],
  temperament: [] as number[],
  grievanceLevel: [] as number[],
  satisfactionLevel: [] as number[],
});
