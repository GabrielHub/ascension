import { soa } from "bitecs";

export const RoomCulture = soa({
  roomInstanceId: [] as string[],
  comfort: [] as number[],
  tension: [] as number[],
  camaraderie: [] as number[],
  tone: [] as string[],
});
