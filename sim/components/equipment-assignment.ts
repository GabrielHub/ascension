import { soa } from "bitecs";

export const EquipmentAssignment = soa({
  operatorId: [] as string[],
  weaponId: [] as string[],
  outfitOverlayId: [] as string[],
  accessoryId: [] as string[],
});
